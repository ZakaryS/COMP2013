import ProductCard from "./ProductCard";

export  default function ProductCardsContainer({ data, ProductQuantity }) {
    return (
        <div className="ProductCardsContainer">
            {data.map((product) => ()
            )}
        </div>
    )
}